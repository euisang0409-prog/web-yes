# Java FileInputStream (바이트 스트림)

## 코드

```java
// 스트림(stream)
// 데이터가 한 방향으로 흐르는 통로이다.
// 파일에 데이터를 저장하거나, 파일에서 데이터를 읽어올 때
// 자바는 데이터를 한 번에 통째로 다루기보다 흐름처럼 조금씩
// 읽고 쓰는 방식으로 처리한다.

// 바이트기반스트림
// 데이터를 1바이트(8비트)로 처리하는 스트림
// 데이터를 아주 작은 단위인 바이트로 읽고 쓴다.

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;

public class FileInputStreamExample {
    public static void main(String[] args) {

        byte[] read = new byte[100];

        // 파일에서 바이트 단위로 데이터를 읽어오는 클래스
        // 파일이 없으면 예외가 발생할 수 있기 때문에 예외처리를 해줘야 한다.
        try {
            FileInputStream fis = new FileInputStream("test.txt");

            // read()
            // 파일에서 1바이트를 읽어서 int로 반환합니다.
            // int로 반환하는 이유
            // 파일의 끝(End Of File)을 표현하기 위해서이다. -> -1

//          int data;
//          while((data = fis.read()) != -1) {
//              System.out.println(data);
//          }

            // read()메서드는 1바이트씩 읽어들이지만
            // read(byte[] b)는 한번에 읽어와서 배열에 집어넣는다.
            fis.read(read);
            
            String result = new String(read);
            
            System.out.println(result);

            // 스트림을 사용하고나면 반드시 닫아야 한다.
            fis.close();

        } catch (FileNotFoundException e) {
            e.printStackTrace();
        } catch (IOException e) {
            e.printStackTrace();
        }

        // read()메서드
        // 1바이트를 읽는다.
        // 읽은 바이트 값을 int로 반환
        // 더이상 읽을 데이터가 없으면 -1을 반환
    }
}
```

---

## 개념 정리

### 스트림 (Stream)

* 데이터가 한 방향으로 흐르는 통로
* 파일을 한 번에 처리하지 않고, 흐름처럼 조금씩 읽고 씀

---

### 바이트 스트림

* 데이터를 1바이트(8비트) 단위로 처리
* 모든 파일을 처리할 수 있음 (텍스트, 이미지 등)

---

### FileInputStream

* 파일을 읽기 위한 클래스
* 바이트 단위로 데이터를 읽음

---

### read() 메서드

#### 1. read()

* 1바이트씩 읽음
* int 타입으로 반환
* 더 이상 읽을 데이터가 없으면 -1 반환

#### 2. read(byte[] b)

* 배열 크기만큼 한 번에 읽음
* 읽은 데이터는 배열에 저장됨

---

### byte[] buffer

```java
byte[] read = new byte[100];
```

* 파일 내용을 담는 저장 공간
* 최대 100바이트까지 저장 가능

---

### 문자열 변환

```java
String result = new String(read);
```

* 바이트 배열 → 문자열 변환

---

### close()

```java
fis.close();
```

* 스트림 사용 후 반드시 닫아야 함
* 메모리 낭비 및 파일 잠금 방지

---

## 핵심 요약

* 스트림 = 데이터 흐름
* FileInputStream = 파일 읽기
* read() = 데이터 읽기
* byte[] = 데이터 저장 공간
* close() = 반드시 호출

---

개념 정리
스트림 (Stream)

데이터가 한 방향으로 흐르는 통로
파일을 한 번에 처리하지 않고, 흐름처럼 조금씩 읽고 씀

바이트 스트림

데이터를 1바이트(8비트) 단위로 처리
텍스트, 이미지 등 모든 파일 처리 가능

FileInputStream

파일을 읽기 위한 클래스
바이트 단위로 데이터를 읽음

read() 메서드
read()
1바이트씩 읽음
int 타입으로 반환
더 이상 읽을 데이터가 없으면 -1 반환
read(byte[] b)
배열 크기만큼 한 번에 읽음
읽은 데이터는 배열에 저장됨
byte[] 배열

파일 내용을 담는 저장 공간
예: byte[] read = new byte[100]; → 최대 100바이트 저장

문자열 변환

new String(byte[])
바이트 데이터를 문자열로 변환

close()

스트림 사용 후 반드시 닫아야 함
메모리 낭비 방지, 파일 잠금 방지

핵심 요약

스트림 = 데이터 흐름
FileInputStream = 파일 읽기
read() = 데이터 읽기
byte[] = 데이터 저장 공간
close() = 반드시 호출


---
