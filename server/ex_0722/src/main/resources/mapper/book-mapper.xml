<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korea.ex_0722.dao.BookDAO">

 <!-- 전체조회 -->
<select id="findAll"
		resultType="com.korea.ex_0722.vo.BookVO">
		
	SELCET
	id,
	title,
	author,
	category,
	price,
	stock
	FROM books
</select>

<!-- 도서 번호 단건 조회 -->
<select id = "findById"
	parameterType="long"
	resultType="com.korea.ex_0722.vo.BookVO">
	select id, title, author, category,price,stock
	from books
	where id=  #{id}
	</select>

<!--  도서 등록 -->
<insert id="insert"
	parameterType="long"
	resultType ="com.korea.ex_0722.vo.BookVO">
	INSERT INTO books (title,author,category,price,stock)
	VALUES (#{title},#{author},#{category},#{price},#{stock})
</insert>

<!-- 도서 수정 -->
<update id="update"
	parmaeterType = "com.korea.ex_0722.vo.BookVO">
	
	UPDATE books
	
	SET
		title = #{title},
		author =#{author},
		category =#{category},
		price = #{price},
		stock = #{stock}

	whier id = #{id}
	
	</update>


