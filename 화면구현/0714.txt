외부평가
필기 9/3 목
실기 9/7 월 (예정)

UI테스트
SQL활용
개발자 환경 구축
응용 SW 기초기술활용
프로그래밍 언어 활용
프로그래밍 언어 응용
어플리케이션 테스트 수행
화면 구현
어플리케이션 배포
학습 모듈에서 이거 위주로만 보기



직전이나 최근 문제
장소 : 학원 강의장 PC

---------------------------------------------------------

제어 역전(Inversion of Control)
우리가 직접 객체를 만드는것이 아니라, 스프링이 객체를 만들고 필요한곳에 주입해주는것

1. 스프링이 객체로 만들도록 클래스에 @Component 어노테이션을 붙인다.

@Component
public class Engine{
	public void start(){ "부릉"}

}

@Component
public class Car{
	Engine engine;

	engine.start();

}


2. 프로젝트를 실행하면 필드로 사용되는 곳에 만들어 놨던 객체를 주입하여  
메서드를 사용할 수 있도록 해준다.

의존성 주입
1. 필드에 직접 주입
2. setter의 매개변수자리에 주입
3. 생성자의 매개변수 자리에 주입

gradle
다양한 프로그래밍 언어를 지원하는 빌드 자동화 도구
의존성 관리, 테스트 실행, 배포, 패키징 등의 빌드 작업을 자동화하며, 유연성과 확장성을 중시한다.

필요한 라이버러리가 있으면 build.gradle파일의 dependencies 부분에 링크를 붙여넣으면 스프링이 자동으로 다운받는다.

mvnrepository에서 링크를 복사해온다.

스프링 프로젝트는 대개 레이어드 아키텍처 패턴으로 이루어져있다.
표현계층 : 클래스 이름뒤에 Controller를 많이 붙인다.
- http
- 응답처리
- 비즈니스 계층의 메서드를 호출
비즈니스계층 : 클래스 이름뒤에 Service를 많이 붙인다.
- 실제 업무를 수행
- 영속 계층의 메서드를 호출
영속계층 : 클레스 or 인터페이스 이름 뒤에 Repository를 많이 붙인다.
- 데이터베이스와 직접 통신하는 계층
자신과 맞닿아있는 계층으로만 이동이 가능하다.

컨트롤러 
클라이언트의  HTTP 요청을 받아서 적절한 기능을 실행하고, 처리 결과를 HTTP 응답으로 반환하는 계층이다.

1. 사용자가 브라우저, 모바일 앱, Postman 같은 프로그램을 통해 서버에 요청을 한다. (URL을 적어서 요청)

요청방식 : GET
요청 주소 : /users/1
전달 데이터 : 1

http://localhost:10000/users/1

@RestController : 해당 클래스가 컨트롤러로 취급되기 위한 어노테이션
ㄴ @Controller + @ResponseBody을 합친 어노테이션

@ResponseBody : 응답결과를 데이터로 주는 어노테이션

@Component(부모) :
|
@Controller(자식)
|	ㄴ @RestController(자손)
@RestController(자손)

@RequestMapping("/users") : 클래스에 붙인다.
@GetMapping("/users") : 메서드에 붙인다.
클라이언트의 HTTP 요청을 Controller 메서드와 연결하는 역할을 한다.

클래스 주소 + 메서드 주소 합쳐지게 된다.

@PathVariable
주소의 일부분을 변수로 하여 데이터를  가져오는 어노테이션

http://localhost:10000/users/10

@GetMapping("users/{id}")
public String findUser(@PathVariable Long id){

}

@RequsertParam 검색 조건이나 옵션에서 사용하는것이 REST API 설계의 관례
URL의 쿼리스트링에 포함된 값을 받아오는 어노테이션
http://localhost:10000/users?name=홍길동

DTO
각 계층간 데이터를 옮기기 위한 객체

엔티티
데이터베이스의 테이블과 1대1로 매치되는 클래스

html -> spring OK 콘솔에서 이름 확인

spring -> html X

CORS(Cross-Origin Resource Sharing)
교차 출처 리소스 공유

출처(Origin)
- 프로토콜, 호스트주소, 포트번호까지 모두 합친 주소를 의미한다.
- 서버의 위치를 찾아가기 위해 필요한 가장 기본적인 것들을 합쳐놓은 것

동일 출처 정책(Same-Origin Policy)
- 브라우저는 기본적으로 보안을 위해 동일 출처 정책이라는 규칙을 따른다.
- 같은 출처에서 로드된 웹사이트만 서로 데이터를 주고받을 수 있다.
- 다른 출처에서 데이터를 요청하는 것을 제한하는 정책
- 이 정책은 보안을 위해 존재하는데, 악의적인 웹사이트가 사용자의 브라우저를 이용해 다른 출처에서 데이터를 가져오지 못하게 막는 역할을 한다.
------------------------------------------------------------------------------------------------------------------------------------------------

이름 인사 출력 프로그램
사용자가 화면에 이름을 입력하고 버튼을 누르면, JS의 fetch()를 이용하여
Springboot의 Controller로 이름을 전송한다.
Controller는 전달받은 이름을 Service로 전달하고, Service는 인사 문장을 생성하여 Controller로 반환한다.
Controller는 그 결과를 다시 브라우저에 반환한다.

프로젝트 : com.korea.test

브라우저에 나올 html ,js는 frontend라고 폴더 만들어서 작성하기

요청주소 GET /api/hello?name=xxx

controller : HelloController
- public String hello();

service : HelloService
- public String getHelloMessage(String name)
- 반환값은 안녕하세요, xxx님!

HTML 작성 조건
6-1. 제목
이름 인사 프로그램

6-2. 입력창
<input
    type="text"
    id="nameInput"
    placeholder="이름을 입력하세요">

6-3. 버튼
id: helloBtn
텍스트: 인사하기

6-4. 결과 영역
<p id="result"></p>

JavaScript 작성 조건

7-1. 요소 가져오기
nameInput
helloBtn
result

7-2. 입력값 처리
const name = nameInput.value.trim();

7-3. 입력값 검사
이름을 입력하세요.

7-4. fetch 요청
fetch(
    "/api/hello?name="
    + encodeURIComponent(name)
)
7-5. 응답 처리
response.text()

---------------------------------------------------------------------------------------------------------------------------------------------------------------

교육 과정 수강생 등록 및 조회 프로그램

HTML과 JavaScript를 사용하여 수강생 정보를 입력하고, Spring Boot 서버에 저장된 수강생 목록을 조회하는 프로그램을 작성합니다.

프론트엔드 파일은 Spring Boot 프로젝트 내부에 저장하지 않고 별도의 폴더에서 실행합니다.

프론트엔드
HTML + JavaScript
      ↓ fetch()
Spring Boot 서버
Controller → Service → ArrayList

데이터베이스는 사용하지 않으며, 입력받은 데이터는 Service 클래스의 ArrayList에 저장합니다.

프로그램 기능

프로그램은 다음 두 가지 기능을 제공합니다.

기능 1. 수강생 목록 조회
서버의 ArrayList에 저장된 모든 수강생을 조회하여 HTML 표에 출력합니다.

기능 2. 수강생 등록
사용자가 입력한 다음 정보를 서버로 전송합니다.

이름
과정명
이메일

서버는 전달받은 정보를 ArrayList에 저장하고 저장 결과를 반환합니다.

화면
student.html
입력 영역
이름 입력창 <input type="text" id="name">
과정명 선택창 <select id="course">
이메일 입력창 <input type="email" id="email">
수강생 등록 버튼 <button id="addBtn">수강생 등록</button>
목록 영역  
목록 조회 버튼 <button id="loadBtn">목록 조회</button>
수강생 목록 표 <tbody id="studentList"></tbody>
student.js
다음 조건을 만족하지 않으면 서버로 요청을 보내지 않습니다.

이름을 입력해야 합니다.
과정명을 선택해야 합니다.
이메일을 입력해야 합니다.
이메일에 @가 포함되어야 합니다.

등록이 성공하면 다음 작업을 수행합니다.

등록 성공 메시지를 출력합니다.
입력창을 초기화합니다.
수강생 목록을 다시 조회합니다.

StudentController.java
- @RequestMapping("/api/students")

조회 기능
@GetMapping
Service의 getStudents()를 호출하고 다음 형태로 반환합니다.

ResponseDTO<StudentDTO>

등록 기능
@PostMapping
JSON 데이터를 다음과 같이 받습니다.

@RequestBody StudentDTO studentDTO

Service의 addStudent()를 호출한 뒤 저장된 수강생 정보를 반환합니다.

StudentService.java 
- @Service를 사용합니다.
- ArrayList<StudentDTO>를 생성합니다.
- 기본 수강생 두 명을 미리 저장합니다.

public List<StudentDTO> getStudents()
전체 수강생 목록을 반환합니다.

public StudentDTO addStudent(StudentDTO studentDTO)
새로운 수강생 정보를 저장하고, 저장된 수강생을 반환합니다.

StudentDTO.java
- id 
- name 
- course 
- email

ResponseDTO.java

전체 수강생 조회
GET http://localhost:8080/api/students

수강생 등록
POST http://localhost:8080/api/students
요청 데이터
{
  "name": "이철수",
  "course": "스프링부트 개발",
  "email": "lee@test.com"
}





















