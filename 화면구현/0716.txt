레이어드 아키텍처의 각 계층은 의존성을 통해 왔다갔다한다.
사용자 화면
ResponseDTO에 응답을 실어서 보냄
표현계층 : controller
dto
서비스 계층 : service
dto
영속계층 : repository
Entity(CRUD)
데이터베이스 : Oracle, MySQL, H2

Spring data JPA
데이터베이스를 프로젝트에서 매우 편리하게 사용할 수 있게 해주는 기술

JDBC만 사용해서 연결할 경우
- 개발자가 직접해야하는 일들이 너무 많다
- 연결, 쿼리문도 직접작성해야한다., SQL 실행, 결과받아서 JAVA객체로 변환, 연결 종료

ORM(Object Relation Mapping)
객체와 관계를 매핑(1:1로 연결)

Java는 데이터를 객체로 다룬다.
DB는 데이터를 테이블로 관리

JPA가 이 둘을 연결 
- 내부적으로 SQL을 실행한다
- 개발자가 SQL을 직접 작성하지 않고 객체 중심으로 데이터를 처리할 수 있다.

Entity
- 테이블이랑 1대1로 매핑되는 클래스
- 클래스에 들어있는 필드 -> 테이블의 속성
- 테이블이 없으면 jpa 설정으로 자동으로 테이블을 만들어준다.
ex) TodoEntity -> todo_entity라는 이름으로 테이블을 만든다.
@Entity : 이 클래스는 JPA가 관리하는 데이터베이스 Entity라고 선언
@Table(name=테이블명) : 테이블 이름을 명확하게 지정하고 싶으면 사용하는 어노테이션

@Id : 해당 필드를 기본키(PK)로 설정
@GeratedValue(strategy = GenerationType.IDENTITY) : 1부터 순차적으로 대입
@Column() : 컬럼의 세부 설정
- name : 컬럼의 이름
- nullable : NULL 허용 여부
- uique : UNIQUE 제약 조건
- length : 문자열의 길이

@CreationTimeStamp : Hibernate에서 제공하는 어노테이션으로 데이터 생성 시간을
자동으로 저장
private LaocalDateTime createAt;

@UpdateTimeStamp : Update쿼리가 실행될 때 시간을 저장해준다.
private LocalDateTime updateAt; 

영속계층에서 메서드를 이용해서 CRUD작업 한다.

JPA에서 기본적으로 제공하는 메서드를 사용하기 위해서는  JpaRepository<T,ID> 인터페이스를 상속받아야 한다.

T : 엔티티 클래스
ID : PK의 타입

기본적으로 제공해주는 메서드
save() : 추가나 업데이트 할 때 사용
findById() : 기본키를 통해서 데이터 하나를 조회 반환형이 Optional이다!!
findAll() : Entity의 모든 데이터를 조회
deleteById() : 기본키 ID를 기준으로 데이터를 삭제한다.
count() : 해당 Entity의 전체 데이터 개수를 반환
existsById() : 특정 ID의 데이터가 존재하는지 확인

쿼리메서드 작성방법
메서드 이름을 규칙에 맞게 작성하면 Spring Data JPA가 쿼리를 자동 생성합니다.

동작 + By + 조건
List<T> findByTitle(String title) : 제목을 조건으로 데이터를 찾는다.

findByTitleContaining(String keyword) : 제목에서 keyword를 포함하는 데이터 찾기

두개의 조건
findByUserldAndDone(String userld, boolean done)

findbyUserIdOrTitle(String userld, String title)

특정 문자열로 시작
findByTitleStartingWith(String str)

범위
초과
findByAgeGreaterThan(int age)

미만
findByAgeLessThan(int age)

findByAgeBetween(int startAge, int endAge)

정렬
findByUserldOrderByAsc(String userId)

findByUserldOrderByTitleAsc(String userId)

----------------------------------------------------------------------------------
도서 관리 REST API 서버 만들기

도서관에서 보유하고 있는 도서 정보를 관리하기 위한 REST API 서버를 개발한다.
화면은 구현하지 않으며, Spring Boot 서버 프로그램만 작성한다.

Client
   ↓
Controller
   ↓
Service
   ↓
Repository
   ↓
Database


데이터베이스는 MySQL을 사용한다.
test_db 사용

---------------------------------------------------
BookEntity 작성 조건

book

다음 필드를 작성한다.

필드명   자료형   설명   조건
id   Long   도서 번호   PK, 자동 증가 id는 GenerationType.IDENTITY 전략
title   String   도서 제목   NULL 불가
author   String   저자   NULL 불가
price   int   도서 가격   
category   String   도서 분야   
stock   int   재고 수량
---------------------------------------------------
BookDTO 작성 조건

클라이언트와 도서 데이터를 주고받기 위한 BookDTO 클래스를 작성한다.

다음 필드를 가진다.

private Long id;
private String title;
private String author;
private int price;
private String category;
private int stock;

Lombok을 이용하여 Getter, Setter, 생성자, Builder를 사용할 수 있도록 작성한다.

또한 BookEntity를 전달받아 BookDTO 객체로 변환할 수 있는 생성자를 작성한다.

다음과 같이 사용할 수 있어야 한다.

BookDTO dto = new BookDTO(entity);
---------------------------------------------------
ResponseDTO 작성 조건

Controller에서 클라이언트에게 데이터를 반환할 때 다음 ResponseDTO를 사용한다.

private String error;
private List<T> data;
---------------------------------------------------
BookRepository 작성 조건

BookRepository는 JpaRepository를 상속받아 작성한다.

Entity 타입은 BookEntity, 기본키 타입은 Long을 사용한다.

제목 검색
도서 제목에 특정 문자열이 포함된 도서를 검색한다.


카테고리 검색
특정 카테고리와 일치하는 도서를 조회한다.

BookService 작성 조건

BookService는 BookRepository를 주입받아 사용한다.

다음 기능을 작성한다.

1. 도서 등록
2. 전체 도서 조회
3. 도서 ID 조회
4. 제목으로 도서 검색
5. 카테고리별 도서 검색
6. 도서 정보 수정
7. 도서 삭제

Service는 Entity를 데이터베이스에 저장하고 조회하는 역할을 Repository에 위임한다.

Controller에는 BookEntity를 직접 반환하지 않고 BookDTO로 변환하여 반환한다.

---------------------------------------------------
BookController 작성 조건

기본 요청 주소는 다음과 같다.

/books

Controller는 BookService를 주입받아 사용한다.

모든 Controller 메서드의 반환형은 다음과 같이 작성한다.

ResponseEntity<?>

모든 결과는 ResponseDTO에 저장한 후 다음 형식으로 반환한다.

return ResponseEntity.ok().body(response);
3
.





























