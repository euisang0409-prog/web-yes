함수
자주 사용하는 코드들의 묶음

function 함수명(매개변수){ ... };

함수명()
const 함수명 = function(매개변수) { ... };

함수명();

화살표 함수
 
(매개변수) => { ...};

매개변수가 없으면 소괄호 생략 불가능
1개일 때 생략 가능
2개일 때는 생략 불가능

명령이 한줄 일 때는 중괄호 생략 가능
return이 있으면 키워드와 함께 중괄호 생략이 가능하다

객체 안에 들어가는 메서드는 화살표함수를 쓰지 않는다!!

let user = {
	name: '홍길동',
	sayHello(){
		console.log(this.name);
	}

	sayHello : () => {console.log(this.name)}

}

user.sayHello();

일반함수의 this
- 누가 호출했는지 보고 this를 결정한다.

화살표 함수의 this
- 자기만의 this가 없다
- 대신 바깥(this)을 그대로 가져온다.

dom 요소의 선택
1. getElementByld("아이디값") : 아이디를 이용한 선택
2. querySelecotr("CSS 선택자") : CSS선ㄴ택자를 이용한 선택
ㄴ 중복되는 클래스값이 있으면 첫번째만 선택
3. querySelectorAll("CSS 선택자") : 전부다 선택 배열처럼 index를
활용하여 각 요소를 선택할 수 있다.

선택한 dom 요소를 조작하기
1. attribute 조작하기
- hasAttribute() : 속성을 가지고 있는지 검사
- getAttribute() : 속성값을 반환
- setAttribute() : 속성을 세팅
- attributes : 태그가 가지고 있는 모든 속성을 가져온다
- removeAttribute()

DOM 객체의 스타일 변경하기
- 선택한 요소객체의 style 프로퍼티를 이용하여 변경하기
let element = document.getElementByld("myElement");

element.style.스타일속성 = "값"

스타일속성은 카멜표현식으로 작성

classList 프로퍼티
- html요소의 class속성을 자바스크립트로 추가, 삭제 변경, 확인할 때 사용하는 프로퍼티이다.

element.classList -> 요소의 class 목록을 가져온다.

element.classList.add("클래스명1","클래스명2") : 클래스를 추가

element.classList.remover("클래스명") : 요소에서 클래스값을 삭제
element.classList.contains("클래스명") : 요소가 해당 클래스값을 가지고 있는지 검사

element.classList.toggle("클래스값") : 있으면 삭제, 없으면 주차

element.classList.replace("기존값","바꾸려는값") : 클래스를 교체할 수 있다.

---------------------------------------------------------------------------------------------------------------
쇼핑몰 상품 목록 페이지 만들기
사용 API
https://dummyjson.com/products
문제

외부 상품 API를 호출하여 쇼핑몰 상품 목록 페이지를 만드시오.

조건
fetch()를 사용하여 상품 데이터를 가져온다.
response.json()을 사용한다.
상품 목록을 카드 형태로 출력한다.
각 상품 카드에는 다음 정보를 출력한다.
상품 이미지
상품명
브랜드
가격
할인율
평점
검색창에 상품명을 입력하면 해당 상품만 출력한다.
카테고리 필터를 만든다.
가격이 100달러 이상이면 고가 상품 배지를 표시한다.
장바구니 담기 버튼을 누르면 장바구니 개수가 증가한다.
API 호출 중 로딩 문구를 출력한다.
----------------------------------------------------------------------------------
실시간 환율 계산기 만들기
문제 설명

외부 API를 이용하여 실시간 환율 계산기를 제작하시오.

사용자는 미국 달러(USD)를 입력한 후 원하는 통화를 선택하여 실시간 환율을 계산할 수 있어야 한다.

환율 데이터는 아래 API를 이용한다.

https://open.er-api.com/v6/latest/USD

반드시 fetch()와 response.json()을 이용하여 데이터를 가져온다.

HTML 작성 조건

다음 요소를 반드시 작성한다.

1. 제목
<h1> 태그를 사용한다.
2. 설명
<p> 태그를 사용한다.
3. USD 입력
<input type="number">를 사용한다.
placeholder를 지정한다.
4. 통화 선택
<select>를 사용한다.
다음 항목을 포함한다.
KRW
JPY
EUR
GBP
5. 계산 버튼
<button> 태그를 사용한다.
6. 결과 출력 영역
<div id="resultBox">를 만든다.
처음에는 화면에 보이지 않아야 한다.

CSS 작성 조건
전체 화면
body 배경색 : #f3f4f6
글꼴 : Arial
계산기 영역
가운데 정렬한다.
너비 : 500px
배경색 : white
padding : 30px
border-radius : 20px
box-shadow
0 5px 15px rgba(0,0,0,0.15)

제목
글자 크기 : 32px
글자색 : #2563eb
가운데 정렬

설명
가운데 정렬
글자색 : gray
입력창(input)
width : 100%
padding : 12px
border-radius : 10px
margin-bottom : 15px

select
input과 동일한 스타일을 적용한다.
버튼
width : 100%
height : 50px
배경색 : #2563eb
글자색 : white
border : none
border-radius : 10px
hover
opacity: 0.8;

결과 영역
처음에는 화면에 보이지 않는다.
배경색 : #eff6ff
padding : 20px
border-radius : 15px
margin-top : 20px
환산 금액
글자 크기 : 30px
굵게 표시한다.
글자색 : #2563eb

JavaScript 작성 조건
1. API 호출
fetch()를 사용한다.
response.json()을 사용하여 JSON 데이터를 객체로 변환한다.
2. 페이지가 실행되면

환율 데이터를 자동으로 가져온다.

3. 입력값 검사

다음 조건에서는 alert()를 출력한다.

입력값이 비어 있는 경우
입력값이 0 이하인 경우
4. 계산 버튼

다음 공식을 이용하여 환산 금액을 계산한다.

입력 금액 × 선택한 통화의 환율
5. 결과 출력

다음 내용을 화면에 출력한다.

기준 통화
선택 통화
입력 금액
적용 환율
환산 금액
환율 업데이트 시간
6. 숫자 출력

toLocaleString()을 사용하여 천 단위 구분기호(,)를 적용한다.

7. 결과 영역 표시

처음에는 결과 영역을 숨겨둔다.

계산 버튼을 누른 후 classList.add()를 사용하여 결과 영역을 화면에 표시한다.

































