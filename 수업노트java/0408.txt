중첩 제네릭
- 제네릭 타입 안에 또 다른 제네릭 타입이 들어간 구조
Box<Pair<String, lnteger>>

복잡한 구조의 표현
Board<article <Intger,Integer>>

코드 가독성과 유지보수에 유리

class Box<T>{
	T item;
}

Box<List<String>> box = new BOx<>();

제네릭 타입 한정
제네릭 타입을 넣을 수 있는 곳이 너무 자유롭다 보니 원하지 않는것도 들어올 수 있음
ㄴ<T extends 상위타입> : 최대 상위타입클래스, 상이타입을 상속하는 클래스들만 들어올 수 있음



제네릭 타입은 
타입변경 x
- 형변환을 성립하기 위해서는 와일카드 문법을 이용해야 한다.
<?>

<? extends T >

<? super T >
매개변수 쪽에서 많이 쓴다.

// <? super T > : T 또는 T의 부모타입만 허용
// 값을 넣는것(add)에 강하다
// 꺼낼때는 Object로만 안전하게 받을 수 있다.