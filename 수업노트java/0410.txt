컬렉션 프레임워크
List 인터페이스
- 순서가 있고, 내용이 중복되도 되는 자료구조
ArrayList

데이터의 추가
add(E e)
add(int index, E e);

데이터의 수정
set(int index. E.e)

데이터의 조회 
get(int index);

데이터의 삭제
remove(int index);

포함하고 있는지 여부
contains(E e);

비어있는지 여부
isEmpty();	
=====================================
Set
- 같은 값은 한 번만 저장할 수 있다.
- 저장된 데이터의 순서를 보장하지 않는 경우가 많다.
- 중복 제거가 필요할 때 자주 사용한다.

자식클래스에 정의된 필드나 메서드를 못쓴다.
first()는 TreeSet클래스

반복자(Iterator)
- 컬렉션 객체를 순회하면서 하나씩 꺼내는 기능
- 지우는 기능

인덱스가 없는 Set에 내용을 꺼낼때 유용하다.
