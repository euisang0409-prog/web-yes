package ex3_class;

// 내가 자바에서 자동차를 어떻게 정의할 것인가를 정하는 클래스
public class Car {
// punlic: 접근 제한자 || class : 클래스 || Car: 클래스명
	
	// 필드(멤버) : 클래스를 통해서 만들어진 객체들이 가질 정보
	// 필드에 미리 값을 설정 해도 됨
	int price;
	String color;
	String brand;
	
	// 속성(필드)
	
	//행위 (메서드)
	
	// 기본 생성자의 정의
	public Car() {
		
	}
	
}
// 하나의 파일에서 여러개의 클래스를 정의하는 것도 가능하다.
class Trucl{};

class BUS{};